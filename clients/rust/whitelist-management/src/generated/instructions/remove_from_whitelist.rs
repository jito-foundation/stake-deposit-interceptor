//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const REMOVE_FROM_WHITELIST_DISCRIMINATOR: u8 = 4;

/// Accounts.
#[derive(Debug)]
pub struct RemoveFromWhitelist {
    pub admin: solana_pubkey::Pubkey,

    pub whitelist: solana_pubkey::Pubkey,

    pub signer_to_remove: solana_pubkey::Pubkey,
}

impl RemoveFromWhitelist {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.admin, true));
        accounts.push(solana_instruction::AccountMeta::new(self.whitelist, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.signer_to_remove,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = RemoveFromWhitelistInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::JITO_WHITELIST_MANAGEMENT_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RemoveFromWhitelistInstructionData {
    discriminator: u8,
}

impl RemoveFromWhitelistInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 4 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for RemoveFromWhitelistInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `RemoveFromWhitelist`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[writable]` whitelist
///   2. `[]` signer_to_remove
#[derive(Clone, Debug, Default)]
pub struct RemoveFromWhitelistBuilder {
    admin: Option<solana_pubkey::Pubkey>,
    whitelist: Option<solana_pubkey::Pubkey>,
    signer_to_remove: Option<solana_pubkey::Pubkey>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl RemoveFromWhitelistBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn admin(&mut self, admin: solana_pubkey::Pubkey) -> &mut Self {
        self.admin = Some(admin);
        self
    }
    #[inline(always)]
    pub fn whitelist(&mut self, whitelist: solana_pubkey::Pubkey) -> &mut Self {
        self.whitelist = Some(whitelist);
        self
    }
    #[inline(always)]
    pub fn signer_to_remove(&mut self, signer_to_remove: solana_pubkey::Pubkey) -> &mut Self {
        self.signer_to_remove = Some(signer_to_remove);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = RemoveFromWhitelist {
            admin: self.admin.expect("admin is not set"),
            whitelist: self.whitelist.expect("whitelist is not set"),
            signer_to_remove: self.signer_to_remove.expect("signer_to_remove is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `remove_from_whitelist` CPI accounts.
pub struct RemoveFromWhitelistCpiAccounts<'a, 'b> {
    pub admin: &'b solana_account_info::AccountInfo<'a>,

    pub whitelist: &'b solana_account_info::AccountInfo<'a>,

    pub signer_to_remove: &'b solana_account_info::AccountInfo<'a>,
}

/// `remove_from_whitelist` CPI instruction.
pub struct RemoveFromWhitelistCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub admin: &'b solana_account_info::AccountInfo<'a>,

    pub whitelist: &'b solana_account_info::AccountInfo<'a>,

    pub signer_to_remove: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> RemoveFromWhitelistCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: RemoveFromWhitelistCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            admin: accounts.admin,
            whitelist: accounts.whitelist,
            signer_to_remove: accounts.signer_to_remove,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.admin.key, true));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.whitelist.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.signer_to_remove.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = RemoveFromWhitelistInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::JITO_WHITELIST_MANAGEMENT_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.admin.clone());
        account_infos.push(self.whitelist.clone());
        account_infos.push(self.signer_to_remove.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `RemoveFromWhitelist` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[writable]` whitelist
///   2. `[]` signer_to_remove
#[derive(Clone, Debug)]
pub struct RemoveFromWhitelistCpiBuilder<'a, 'b> {
    instruction: Box<RemoveFromWhitelistCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> RemoveFromWhitelistCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(RemoveFromWhitelistCpiBuilderInstruction {
            __program: program,
            admin: None,
            whitelist: None,
            signer_to_remove: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.admin = Some(admin);
        self
    }
    #[inline(always)]
    pub fn whitelist(&mut self, whitelist: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.whitelist = Some(whitelist);
        self
    }
    #[inline(always)]
    pub fn signer_to_remove(
        &mut self,
        signer_to_remove: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.signer_to_remove = Some(signer_to_remove);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = RemoveFromWhitelistCpi {
            __program: self.instruction.__program,

            admin: self.instruction.admin.expect("admin is not set"),

            whitelist: self.instruction.whitelist.expect("whitelist is not set"),

            signer_to_remove: self
                .instruction
                .signer_to_remove
                .expect("signer_to_remove is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct RemoveFromWhitelistCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    whitelist: Option<&'b solana_account_info::AccountInfo<'a>>,
    signer_to_remove: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
